{
  "name": "corsproxy",
  "version": "1.0.0",
  "description": "proxuy for api calls to other domain bypassing cors",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mihir Sasmal",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.85.0"
  }
}
